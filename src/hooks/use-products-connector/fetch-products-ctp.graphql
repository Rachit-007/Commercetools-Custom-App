query FetchProducts($limit: Int!, $offset: Int!, $sort: [String!]) {
  products(limit: $limit, offset: $offset, sort: $sort) {
    total
    count
    offset
    results {
      id
      version
      key
      productType {
        name
      }
      masterData {
        current {
          name(locale: "en")
        }
        hasStagedChanges
        published
      }
      createdAt
      lastModifiedAt
    }
  }
}
